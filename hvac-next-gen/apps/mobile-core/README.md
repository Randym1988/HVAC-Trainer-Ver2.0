# Mobile Core

Reserved for the Capacitor shell and native plugin integration.

Recommended next steps:

1. Initialize a React/Vue app in this folder.
2. Add Capacitor with `npx cap init`.
3. Sync iOS/Android platforms with `npx cap sync`.
