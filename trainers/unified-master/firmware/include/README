Heat-pump firmware header directory.

Use this folder for reusable declarations shared across multiple source files.

Current project status:
- most implementation remains in `src/main.cpp`
- move declarations here only when splitting modules to avoid excessive global
	surface area
