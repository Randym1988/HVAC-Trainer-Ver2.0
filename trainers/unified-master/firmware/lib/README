Heat-pump firmware private library directory.

Current project status:
- no custom private libraries are required
- heat-pump logic currently lives in `src/main.cpp` for easier field
  troubleshooting

If new reusable components are extracted, add them here under a dedicated
subfolder per module.
