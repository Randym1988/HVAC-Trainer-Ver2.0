Heat-pump firmware test directory.

Recommended coverage for this project:
- thermostat call intake and telemetry propagation
- heating/cooling pressure model transitions
- HPS/LPS trip and reset hysteresis

Run tests with:

```bash
pio test -e usb
```
