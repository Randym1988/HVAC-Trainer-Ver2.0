Furnace firmware test directory.

Recommended coverage for this project:
- furnace state transitions and lockout behavior
- pressure switch trip/reset thresholds
- thermostat call parsing and debounce behavior

Run tests with:

```bash
pio test -e usb
```
